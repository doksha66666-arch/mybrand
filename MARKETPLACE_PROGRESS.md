# MYBRAND Marketplace — تتبع التقدم

هذا الملف يوثّق تقدّم تحويل المشروع من متجر واحد إلى Marketplace متعدد التجار، حتى تقدر أي محادثة قادمة (أو أي مطوّر) تكمل من حيث توقفنا بالظبط.

## ✅ المرحلة 1 (منفَّذة بالكامل) — Models + تسجيل/موافقة التاجر + العمولة

### Models جديدة
- `backend/models/Merchant.js` — بيانات النشاط التجاري، الحالة (pending/approved/suspended)، نسبة العمولة، سبب الإيقاف.

### Models معدَّلة (بدون كسر أي بيانات موجودة)
- `User.js` — أُضيف `'merchant'` لقائمة الأدوار (كانت `customer`/`admin` فقط).
- `Product.js` — أُضيف: `merchant` (ref، فارغ = منتج مملوك للمنصة)، `status` (draft/pending/approved/rejected/hidden/out_of_stock — **افتراضيًا approved** حتى لا تختفي المنتجات الحالية)، `rejectionReason`، `commissionRateOverride`.
- `Order.js` — كل عنصر في `items` أصبح يحمل: `merchant`, `commissionRate`, `commissionAmount`, `merchantAmount`. وأُضيف على مستوى الطلب: `totalCommissionAmount`, `totalMerchantAmount`.

### ⚠️ نقطة توافق حرجة تم التعامل معها
منتجات MongoDB الموجودة فعليًا (قبل هذا التعديل) **ليس لها حقل `status` مخزَّن في المستند نفسه**. فلاتر البحث في `productController.js` (`getProducts`, `getProductBySlug`) تستخدم:
```js
$or: [{ status: 'approved' }, { status: { $exists: false } }]
```
بدل `status: 'approved'` مباشرة، عشان المنتجات القديمة تفضل ظاهرة للعميل. **لو حصل أي تعديل مستقبلي على هذا الفلتر، لازم يفضل يحافظ على هذا المنطق.**

### Backend API جديد
- `POST /api/merchants/register` — تسجيل تاجر جديد (عام)، ينشئ `User(role=merchant)` + `Merchant(status=pending)`.
- `GET/PUT /api/merchants/me` — بروفايل التاجر نفسه.
- `GET /api/merchants` — Admin: كل التجار (فلترة بالحالة).
- `GET /api/merchants/:id` — Admin: تفاصيل تاجر + إحصائيات (مبيعات، عمولة، صافي مستحق) محسوبة Live من الطلبات.
- `PUT /api/merchants/:id/status` — Admin: قبول/إيقاف تاجر.
- `PUT /api/merchants/:id/commission` — Admin: تحديد نسبة العمولة.
- `GET /api/products/mine` — التاجر: كل منتجاته (كل الحالات).
- `GET /api/products/pending` — Admin: المنتجات قيد المراجعة.
- `PUT /api/products/:id/review` — Admin: قبول/رفض منتج (مع سبب الرفض).
- `POST/PUT/DELETE /api/products` — أصبحت تعمل للأدمن (زي ما هي) وللتاجر المعتمد (على منتجاته فقط، وأي تعديل يرجّع المنتج لـ pending تلقائيًا).
- `GET /api/orders/merchant/mine` — التاجر: طلباته فقط (عناصره هو بس من كل طلب، بدون رؤية بيانات تجار آخرين).
- `createOrder` أصبح يحسب `commissionRate/commissionAmount/merchantAmount` تلقائيًا لكل عنصر وقت إنشاء الطلب.

### Middleware جديد (`middleware/auth.js`)
- `merchantOnly` — يتطلب حساب تاجر (بغض النظر عن حالة الاعتماد، عشان يشوف حالة طلبه).
- `approvedMerchantOnly` — يتطلب تاجر معتمد فعليًا (يُستخدم على إضافة/تعديل/حذف المنتجات).
- `protect` أصبح يحمّل `req.merchant` تلقائيًا لو الحساب تاجر.

### ما لم يتغيّر (تأكيد التوافق)
- كل مسارات `/api/auth`, `/api/categories`, `/api/cart`, `/api/wishlist`, `/api/customers`, `/api/upload`, `/api/config` — **بدون أي تعديل إطلاقًا**.
- استجابات الـ API للعميل العادي (`web`, `mobile`) — نفس الشكل تمامًا، فقط حقول إضافية اختيارية.
- لوحة التحكم (`admin-dashboard`) الحالية — تعمل بدون أي تعديل مطلوب (كل إضافة API إضافية، مفيش كسر).

---

## ✅ المرحلة 2 (منفَّذة بالكامل) — واجهات الأدمن والتاجر

### لوحة الأدمن (`admin-dashboard`) — إضافات جديدة
- **صفحة "التجار"** (`/merchants`) — قائمة كل التجار مع فلترة بالحالة (الكل/قيد المراجعة/معتمد/موقوف).
- **صفحة تفاصيل التاجر** (`/merchants/:id`) — إحصائيات Live (منتجات، طلبات، مبيعات، عمولة، صافي مستحق) + أزرار قبول/إيقاف (مع سبب) + تحديد نسبة العمولة.
- **صفحة "مراجعة المنتجات"** (`/products-review`) — كل منتجات التجار قيد المراجعة، مع قبول/رفض (والرفض يتطلب سبب يظهر للتاجر).
- **صفحة "المنتجات"** الحالية تطوّرت: بقت تستخدم `/api/products/admin/all` (endpoint جديد بيرجّع كل المنتجات بكل حالاتها، مش بس المعتمدة) وبتعرض عمود "التاجر" وعمود "الحالة" لكل منتج.
- **القائمة الجانبية** (`Layout.jsx`) — روابط جديدة مع عداد (Badge) تلقائي لعدد التجار الجدد والمنتجات قيد المراجعة.
- **لوحة البيانات الرئيسية** تطوّرت بالكامل: عملاء، تجار (+قيد المراجعة)، منتجات (+قيد المراجعة)، طلبات، إجمالي المبيعات، إجمالي عمولات MYBRAND، صافي مستحق التجار — كلها أرقام حقيقية من قاعدة البيانات.

### لوحة التاجر (`merchant-dashboard`) — تطبيق جديد بالكامل
مشروع React+Vite مستقل (بورت 5175)، بنفس هوية MYBRAND البصرية:
- **تسجيل** (`/register`) — نموذج انضمام تاجر جديد (بيانات شخصية + بيانات نشاط تجاري).
- **دخول** (`/login`) — يستخدم نفس `/api/auth/login` مع التحقق من أن الحساب `role=merchant`.
- **بانر حالة** يظهر أعلى كل الصفحات لو الحساب `pending` أو `suspended` (مع سبب الإيقاف لو موجود).
- **لوحة البيانات** — مبيعات، طلبات، منتجات، منتجات قيد المراجعة، عمولة، صافي مستحق.
- **المنتجات** — إضافة/تعديل/حذف (تظهر فقط لو الحساب معتمد)، مع Badge لحالة كل منتج وسبب الرفض لو موجود، ورفع صور (نفس نظام Cloudinary/محلي).
- **الطلبات** — طلباته فقط، وعناصره فقط من كل طلب (خصوصية كاملة عن باقي التجار).
- **المبيعات** — جدول تفصيلي لكل طلب (مبيعات/عمولة/صافي) + إجماليات.
- **العمولة** — عرض فقط (مش قابلة للتعديل من التاجر نفسه) + ملخص مالي.
- **البروفايل** — تعديل بيانات النشاط التجاري.

### Backend — إضافات مطلوبة لدعم الواجهات
- `GET /api/products/admin/all` — Admin فقط، كل المنتجات بدون فلترة.
- `GET /api/orders/stats/summary` — Admin فقط، إجماليات مبيعات/عمولات على مستوى المنصة (Aggregation، مش سحب كل الطلبات للفرونت).
- `GET /api/merchants/me` أصبح يرجّع `stats` كمان (مش بس بيانات البروفايل).
- `POST /api/upload` — كان مقصور على الأدمن فقط، بقى متاح للتاجر أيضًا (كان لازم يرفع صور منتجاته).

### نقاط مهمة للمرحلة القادمة
- لسه مفيش رابط "سجّل كتاجر" ظاهر من `web` (المتجر العام) — التسجيل حاليًا بس من `merchant-dashboard/register` مباشرة.
- تعديل التاجر لمنتج معتمد بيرجّعه لحالة `pending` تلقائيًا (قرار تصميم مقصود لضمان مراجعة أي تغيير) — لو الأدمن عايز سلوك مختلف (مثلاً: تعديل السعر/المخزون بس من غير إعادة مراجعة)، محتاج تحديد إيه بالظبط.

---

## ✅ المرحلة 2.5 (منفَّذة بالكامل) — ربط تسجيل التاجر بالواجهة العامة للمتجر (`web`)

### الفكرة
صفحة `/register` في `web` بقت فيها اختيار واضح: "إنشاء حساب عميل" أو "طلب الانضمام كتاجر"، بدل ما يكون التسجيل كتاجر مقصور على `merchant-dashboard` بس.

### `backend`
- `models/Merchant.js` — أُضيف حقل `storeName` (اسم المتجر كما يظهر للعملاء، منفصل عن `businessName` اسم النشاط التجاري القانوني). افتراضيًا `''` عشان التجار الحاليين (لو موجودين) ما ينكسروش.
- `controllers/merchantController.js` — `registerMerchant` و`updateMyMerchantProfile` أصبحا يستقبلا/يحدّثا `storeName`. التسجيل بقى يتطلب `storeName` بالإضافة لباقي الحقول المطلوبة.

### `web` (المتجر العام)
- `pages/RegisterPage.jsx` — أُعيدت كتابتها بالكامل: تبويب علوي (عميل/تاجر). نموذج العميل هو **نفسه القديم بالضبط** بدون أي تغيير في السلوك. نموذج التاجر جديد بالحقول: الاسم، البريد، الهاتف، كلمة المرور، اسم النشاط التجاري، اسم المتجر، وصف النشاط، وبيانات إضافية اختيارية (هاتف/عنوان النشاط). بعد الإرسال يظهر تأكيد "قيد المراجعة" + رابط لصفحة دخول `merchant-dashboard`.
- `context/AuthContext.jsx` — أُضيفت دالة `registerMerchant` (تستدعي `/api/merchants/register` بدون ما تسجّل دخول المستخدم تلقائيًا على المتجر العام، حفاظًا على فصل جلسة العميل عن التاجر). كمان بقى يحمّل بيانات `merchant` (الحالة) تلقائيًا لو المستخدم المسجّل دخوله `role=merchant`.
- `components/Header.jsx` — رابط "كن تاجرًا" ظاهر للجميع (يوجّه لـ `/register?type=merchant`). ولو المستخدم مسجّل دخوله بحساب تاجر: زر واضح "🏪 لوحة التاجر" لو معتمد (يفتح `merchant-dashboard` في تبويب جديد)، أو badge بحالة الطلب (قيد المراجعة/موقوف) لو مش معتمد بعد.
- `components/Footer.jsx` — رابط إضافي "سجّل كتاجر معنا".
- `api/client.js` + `.env.example` — أُضيف `VITE_MERCHANT_DASHBOARD_URL` (افتراضيًا `http://localhost:5175`) لضبط رابط لوحة التاجر عند النشر.

### `merchant-dashboard` و`admin-dashboard`
- نموذج تسجيل التاجر وصفحة البروفايل في `merchant-dashboard` أُضيف لهما حقل "اسم المتجر" (لازم يتوافقا مع الحقل الجديد المطلوب في الـ backend).
- صفحتا "التجار" و"تفاصيل التاجر" في `admin-dashboard` بقتا تعرضا "اسم المتجر" بجانب "اسم النشاط".

### ما لم يتغيّر
- تسجيل العملاء (`/api/auth/register`) — **بدون أي تعديل**.
- منطق الموافقة/العمولة/الصلاحيات في `middleware/auth.js` — **بدون أي تعديل**.
- التاجر ما زال غير قادر على تغيير `commissionRate` من أي واجهة.

---

## ⏳ المراحل القادمة (لم تُنفَّذ بعد)

### المرحلة 3 — طرق الدفع كـ Model منفصل
- `PaymentMethod` model بدل الـ enum الثابت الحالي، مع لوحة تحكم لإضافة/تعديل/تفعيل طرق الدفع.

### المرحلة 4 — العروض والخصومات (Coupon)
- `Coupon` model: نسبة/قيمة خصم، نطاق (منتج/قسم/تاجر/المتجر كله)، تواريخ، حد أدنى/أقصى.

### المرحلة 5 — الفعاليات والحملات (Campaign) + البنرات (Banner)
- `Campaign` model وربطه بمنتجات/أقسام/تجار.
- `Banner` model لإدارة محتوى الصفحة الرئيسية بدون تعديل كود.

### المرحلة 6 — تحسينات الطلبات متعددة التجار
- عرض الطلب مقسّم بصريًا حسب التاجر في واجهة العميل ولوحة الأدمن.
- Dashboard متقدم في الأدمن (عدد عملاء/تجار/منتجات، إجمالي مبيعات وعمولات).

---

## كيفية التشغيل بعد هذا التعديل
نفس خطوات `README.md` تمامًا — لا تغيير في طريقة التشغيل. أول مرة بعد سحب هذا التحديث، لازم فقط:
```bash
cd backend
npm run dev
```
Mongoose هينشئ الحقول الجديدة تلقائيًا لأي مستند جديد، والمستندات القديمة هتفضل شغّالة بفضل منطق التوافق أعلاه.
