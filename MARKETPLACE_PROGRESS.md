# MYBRAND Marketplace — تتبع التقدم

هذا الملف هو مرجع الحالة الفعلية للمشروع، ويجب تحديثه مع كل مرحلة مكتملة حتى تستمر أي جلسة تطوير لاحقة من آخر نقطة صحيحة.

## ✅ المرحلة 1 — Marketplace Models + التجار + العمولة
- نموذج `Merchant` مع حالات `pending/approved/suspended` ونسبة العمولة وسبب الإيقاف.
- `User` يدعم دور `merchant`.
- `Product` يدعم `merchant`, `status`, `rejectionReason`, و`commissionRateOverride` مع الحفاظ على توافق المنتجات القديمة.
- `Order.items` يحفظ التاجر والعمولة وصافي مستحق التاجر، مع إجماليات على مستوى الطلب.
- تسجيل/موافقة/إيقاف التجار، وإدارة منتجات التاجر وصلاحياته.
- عزل طلبات التاجر: التاجر يرى عناصره فقط من الطلبات المشتركة.

## ✅ المرحلة 2 — لوحات الأدمن والتاجر
### Admin Dashboard
- إدارة التجار وحالاتهم وعمولاتهم.
- مراجعة منتجات التجار وقبول/رفض المنتجات.
- إدارة المنتجات بكل الحالات.
- إحصائيات العملاء والتجار والمنتجات والطلبات والمبيعات والعمولات وصافي مستحق التجار.

### Merchant Dashboard
- التسجيل والدخول وحالة الحساب.
- إدارة المنتجات ورفع الصور.
- الطلبات الخاصة بالتاجر فقط.
- المبيعات والعمولة وصافي المستحق.
- الملف التجاري.

## ✅ المرحلة 2.5 — التسجيل من المتجر العام
- اختيار التسجيل كعميل أو كتاجر من `web`.
- `storeName` أصبح جزءًا من بيانات التاجر.
- رابط "كن تاجرًا" في الواجهة العامة.
- ربط جلسة التاجر بلوحة التاجر مع الحفاظ على فصل جلسة العميل.

## ✅ المرحلة 3 — طرق الدفع كـ Model منفصل
تم تنفيذها فعليًا.
- `PaymentMethod` model يدعم `cod`, `manual_transfer`, و`gateway`.
- الإدارة تتحكم في التفعيل والترتيب والاسم والوصف والتعليمات وقيمة التحويل.
- واجهة Checkout تجلب طرق الدفع المتاحة من الخادم.
- الخادم يرفض أي طريقة غير مفعلة أو غير مكتملة الإعداد.
- بوابات الدفع الإلكترونية الحقيقية لا تُعتبر مدفوعة ولا تُقبل قبل ربط Gateway حقيقي.
- طرق التحويل اليدوي تحتاج قيمة وجهة تحويل قبل قبول الطلب.

## ✅ المرحلة 4 — الكوبونات والخصومات
تم تنفيذها فعليًا.
- التحقق من القسيمة على الخادم.
- دعم نسبة أو قيمة ثابتة مع حد أدنى وحد أقصى للخصم.
- تواريخ الصلاحية وحد الاستخدام.
- قسائم مخصصة لمستخدمين بعينهم.
- حجز استخدام القسيمة وقت إنشاء الطلب وإرجاع الاستخدام عند فشل العملية أو إلغاء الطلب.
- منع تجاوز قيمة الخصم لإجمالي المنتجات.

## ✅ المرحلة 5 — الحملات والبنرات
موجودة ضمن المشروع الحالي وتُدار من لوحات الإدارة حسب الـModels والـControllers الحالية.
- `Campaign` و`Banner` موجودان.
- إدارة المحتوى والرفع مرتبطة بنظام الوسائط الحالي.

## ✅ المرحلة 6 — تحسين دورة الطلب متعددة التجار
تم تنفيذ الجزء الأساسي والحساس منها.
- الطلب الواحد يدعم عناصر من عدة تجار مع عمولة منفصلة لكل عنصر.
- لوحة التاجر تعرض عناصر التاجر فقط.
- يوجد `MerchantOrderStatus` لحالة تجهيز كل تاجر داخل الطلب.
- حالات التجهيز: `confirmed → packed → ready` ولا يمكن الرجوع للخلف.
- الإدارة ترى تجميع حالة التجهيز لكل تاجر داخل الطلب.
- واجهة العميل تعرض الطلب وحالته العامة.

## ✅ تحسينات الطلبات التي أضيفت لاحقًا
### لقطات تاريخية للطلب
عند إنشاء الطلب يتم حفظ:
- `nameSnapshot`
- `imageSnapshot`
- `productCodeSnapshot`
- `notesSnapshot`
- بيانات التاجر والتقييم والعمولة وقت الطلب
وبالتالي لا تعتمد تفاصيل الطلب القديم على بيانات المنتج الحالية بعد تغييره.

### الإلغاء الآمن
- العميل يستطيع الإلغاء فقط في `pending` أو `confirmed`.
- الإلغاء محمي بملكية الطلب.
- تتم إعادة المخزون والقسيمة ونقاط الولاء إن وجدت ضمن Transaction.
- الدفع المعلق يتحول إلى `failed` عند الإلغاء.
- الإدارة يمكنها إلغاء الطلب وفق قواعد الحالة نفسها.

### مخزون الـVariants
- يوجد مزامنة تلقائية بين مخزون المتغيرات و`product.stock` عند تحديث المتغيرات.
- تمت إضافة تغطية `updateOne` أيضًا حتى لا يبقى `product.stock` قديمًا بعد إعادة المخزون من مسارات الإلغاء.

### حماية إضافية للطلبات والمدفوعات
- توليد `orderNumber` يعيد المحاولة عند اصطدام رقم الطلب بقيد `unique` بدل فشل إنشاء الطلب بسبب تصادم عشوائي نادر.
- تحديث حالة الدفع في لوحة الإدارة أصبح مقيدًا بانتقالات مسموحة بدل قبول أي حالة عشوائية.

### الواجهة
- حالة `pending` ظاهرة كمرحلة فعلية في تتبع الطلب.
- الطلب الملغي يظهر كحالة نهائية مستقلة.
- صفحات الطلب والتتبع تستخدم snapshots التاريخية عند توفرها.
- Tracking يدعم إلغاء الطلب قبل التجهيز.

## ✅ البنية التحتية والنشر
- Railway ينشر الخدمات الأربع من `main`:
  - Backend `mybrand`
  - Storefront `enchanting-miracle`
  - Merchant Dashboard `independent-flow`
  - Admin Dashboard `lovely-serenity`
- آخر نشر مؤكد وقت تحديث الملف كان ناجحًا على الخدمات الأربع.
- CI في `.github/workflows/ci.yml` يتحقق من JavaScript في الـbackend ويبني تطبيقات الواجهات الأربع.
- تم إصلاح Railway healthcheck للواجهات.
- تم تحسين استرداد LIVE viewer heartbeat بعد إعادة تشغيل الخادم.
- تم فصل rate limit الخاص بمسارات LIVE عن الحد العام.
- تمت إضافة favicon و`robots.txt` مناسب للمتجر ومنع فهرسة لوحات الإدارة والتاجر.

## ⏳ النقاط التالية ذات الأولوية
1. اختبار دورة Checkout/Orders فعليًا على الإنتاج بحالات COD + تحويل يدوي + كوبون + مخزون variants + إلغاء.
2. مراجعة تجربة الطلب متعدد التجار في واجهة العميل، خصوصًا عرض حالة كل تاجر بشكل واضح بجانب الحالة العامة.
3. مراجعة اتساق استرداد المدفوعات اليدوية مع تغيير `paymentStatus` في لوحة الإدارة، ووضع قواعد أكثر صرامة مرتبطة بحالة الطلب الفعلية.
4. إنشاء اختبارات آلية حقيقية للـAPI ومسارات الطلب بدل الاكتفاء بفحوصات بناء المشروع.

## ⚠️ ملاحظات توافق يجب الحفاظ عليها
- المنتجات القديمة التي لا تحتوي `status` يجب أن تظل متاحة للعميل عبر منطق `approved OR status missing`.
- لا يُسمح للعميل أو التاجر برؤية حقول العمولة وصافي التاجر غير المخصصة له.
- لا تعتبر أي Gateway دفعًا ناجحًا قبل وجود تكامل حقيقي مع مزود الدفع.
- أي تعديل مستقبلي على المخزون يجب أن يحافظ على اتساق `variants.*.stock` و`product.stock`.
